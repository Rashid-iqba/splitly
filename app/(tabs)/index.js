import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import AllChartsScreen from '../../components/chats&grapgh';
const index = () => {
  return (
    <View style={{flex:1}}>
      <Text>index</Text>
      <AllChartsScreen/>
    </View>
  )
}

export default index

const styles = StyleSheet.create({})